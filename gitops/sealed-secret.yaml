apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: taskapp-secrets
  namespace: taskapp
  annotations:
    sealedsecrets.bitnami.com/managed: "true"
spec:
  encryptedData:
    POSTGRES_PASSWORD: AgBy3i4... (encrypted)
    SECRET_KEY: AgB7k9... (encrypted)
  template:
    metadata:
      name: taskapp-secrets
      namespace: taskapp
    type: Opaque
