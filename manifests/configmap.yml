apiVersion: v1
kind: ConfigMap
metadata:
  name: taskapp-config
  namespace: taskapp
data:
  POSTGRES_USER: taskapp_user
  POSTGRES_DB: taskapp
  FLASK_ENV: production
  CORS_ORIGINS: "http://localhost:8080,http://localhost:3000"